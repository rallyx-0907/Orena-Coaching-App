# Re-pin, 2026-09-22 (second sync of the day)

The human reported that the interface was being built against an out-of-date
design. It was. This cache was pinned on 2026-09-21 and three of its files had
moved at the source since.

## What changed

| File | Pinned | Source | |
| --- | ---: | ---: | --- |
| `screens/Orena-Progress.dc.html` | 78 947 | **101 462** | +22 515 bytes - the screen was reworked |
| `screens/Orena-UI-Baseline.dc.html` | 91 651 | **90 759** | the ground changed |
| `screens/Orena-Listening.dc.html` | 95 578 | **95 302** | small change |

Unchanged, verified byte for byte: `Orena-Home-Discover`, `Orena-Reading`,
`Orena-Speaking`, `Orena-Vocabulary`, `Orena-Writing`.

`PINS.tsv` is recomputed from the files on disk, so the record cannot drift
from what sits beside it.

## The ground, which is what went wrong

`Orena UI Baseline.dc.html` is the master preview named by `UI_BASELINE.md` §6,
and its body carries the product's own ground. It now reads:

```
background-color: #0B0A0F
radial-gradient(76% 52% at 62% -6%, rgba(168,84,190,0.32), transparent 72%)
radial-gradient(46% 32% at 92% 2%, rgba(196,104,196,0.20), transparent 74%)
linear-gradient(180deg, #17101F 0%, #0D0A12 44%, #0A090D 100%)
background-attachment: fixed
```

Two magenta radials off the top over a vertical fall to near-black. D-071 had
pinned `#0A0722` under seventeen layers, which was true of the master preview
on 2026-09-21 and is not true now.

**Three files in the design still disagree with the master preview about the
ground, and they are all older:**

| Says | Where |
| --- | --- |
| `#050310` + a twelve-layer cosmic field | the eight screen files' own `body`, `UI_BASELINE.md` §1 |
| `#050408` + a three-direction field + a 280-420px halo | `Orena Glass System.dc.html` §02 |
| `#0B0A0F` + two magenta radials + a vertical fall | **`Orena UI Baseline.dc.html`** |

The screen files' `body` is the design document's own page - the canvas their
frames are pinned on - not the app's ground, so it was never the right source
for it. The Glass System's values are the earlier system. The master preview is
what the product looks like, and it is what `theme.css` carries.

This cost three wrong applications in one session before the master preview was
read. Anyone reaching for the ground should read the master preview and nothing
else; the other two are kept because the rest of their content is still
canonical.

## Not re-pinned

`tokens.json` is a faithful copy of `ui-baseline/foundations/tokens.json` at the
source, and that file still states `canvas: #050310`. It is left as it is - the
cache's job is to be a faithful copy - and the disagreement above is the record.
