import assert from 'node:assert/strict';
import {readFileSync} from 'node:fs';
const read=p=>readFileSync(new URL(p,import.meta.url),'utf8');
const backend=read('../writing_coach/media_interaction.py');
const app=read('../app.py');
const api=read('../static/orena/infrastructure/api.js');
const encounter=read('../static/orena/ui/encounter.js');
assert.match(backend,/class ContextualDictionaryIn/);
assert.match(backend,/Selected text must come from the supplied learner context/);
assert.match(backend,/contextual_dictionary_unavailable/);
assert.match(app,/app.include_router\(contextual_dictionary_router\)/);
assert.match(api,/contextualDictionary:/);
assert.match(encounter,/api.contextualDictionary/);
assert.match(encounter,/text.includes\(phrase\(\)\)/);
assert.match(encounter,/!result.available/);
assert.doesNotMatch(encounter,/JSON.stringify\(meaning\)/);
console.log('Contextual dictionary: grounded lookup and honest unavailable state PASS');
