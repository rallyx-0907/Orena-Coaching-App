.venv/
__pycache__/
data/*.db
# Fetched pronunciation and its licence record: a cache of somebody else's
# files, rebuilt on demand from Wikimedia. It is not source, and the licence
# travels with the bytes rather than with the repository.
data/word_audio/
.env
.env.*
!.env.example
update.log
backups/
.upgrade_backups/

# Updater backups
*.v*-backup-*
.upgrade_backups/

# Playwright MCP scratch output from local browser acceptance runs.
.playwright-mcp/
